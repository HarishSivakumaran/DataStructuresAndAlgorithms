public class Check extends TreeHeight {
    protected void nothing(){
        TreeHeight treeHeight = new TreeHeight();
        treeHeight.printArray(new int[]{12345,234});
        Check check = new Check();
        check.printArray(new int[]{1,2,3,4,5,5});
    }
}
